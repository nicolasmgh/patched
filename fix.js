const fs = require('fs'); let b = fs.readFileSync('apps/api/src/services/admin.service.js', 'utf8'); b = b.replace('data: { active },\\n    });\\n\\n    if (active) {', 'data: { active },\\n    });\\n\\n    await prisma.notification.create({ data: { userId, type: active ? \USER_UNBANNED\ : \USER_BANNED\, message: active ? \Tu cuenta ha sido reactivada.\ : \Tu cuenta ha sido suspendida por incumplir las normas.\ } });\\n\\n    if (active) {'); fs.writeFileSync('apps/api/src/services/admin.service.js', b);
